<Window x:Class="HernadVed.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:HernadVed.ViewModels"
        mc:Ignorable="d"
        Title="Hernád-Véd - Bejelentkezés" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20">
            <StackPanel>
                <TextBlock Text="Hernád-Véd" 
                          FontSize="28" 
                          FontWeight="Bold" 
                          Foreground="White"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Diszpécser Rendszer" 
                          FontSize="16" 
                          Foreground="White"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Login Form -->
        <StackPanel Grid.Row="1" 
                   Margin="50,30"
                   VerticalAlignment="Center">
            
            <Label Content="Email cím:" FontWeight="SemiBold"/>
            <TextBox x:Name="EmailTextBox"
                    Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>

            <Label Content="Jelszó:" FontWeight="SemiBold" Margin="0,15,0,0"/>
            <PasswordBox x:Name="PasswordBox"
                        PasswordChanged="PasswordBox_PasswordChanged"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>

            <!-- Error Message -->
            <TextBlock Text="{Binding ErrorMessage}" 
                      Foreground="Red"
                      FontSize="12"
                      Margin="0,10,0,0"
                      TextWrapping="Wrap"
                      Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <!-- Login Button -->
            <Button Content="Bejelentkezés" 
                   Command="{Binding LoginCommand}"
                   Margin="0,20,0,0"
                   Height="40"
                   IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>

            <!-- Loading Indicator -->
            <TextBlock Text="Bejelentkezés folyamatban..." 
                      HorizontalAlignment="Center"
                      Margin="0,10,0,0"
                      FontStyle="Italic"
                      Foreground="#666"
                      Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</Window>
